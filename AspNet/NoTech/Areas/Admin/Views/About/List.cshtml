@model IList<About>

@if (TempData["Success"] != null)
{
    <div class="alert alert-primary" role="alert">
        @TempData["Success"]
    </div>
}

<div>
    <a href="/Admin/About/Create" class="btn btn-success">Add</a>
</div>

<div class="table-responsive scrollbar">
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Title</th>
                <th scope="col">Text</th>
                <th scope="col">List</th>
                <th scope="col">SubHeading</th>
                <th scope="col">Image 1</th>
                <th scope="col">Image 2</th>

                <th class="text-end" scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td>@item.ID</td>
                <td>@item.Title</td>
                <td>@item.Text</td>
                <td>@item.List</td>
                <td>@item.Subheading</td>
                <td>
                    <img width="200" height="150" src="~/assets/images/resources/@item.Image1">
                </td>
                <td>
                    <img width="200" height="150" src="~/assets/images/resources/@item.Image2">
                </td>

                <td class="text-end">
                    <div>
                        <a href="/Admin/About/Edit/@item.ID"><i class="fas fa-edit"></i></a>
                        <a href="/Admin/About/Delete/@item.ID" onclick="return confirm('Are you delete?')"><i class="fas fa-trash-alt"></i></a>
                    </div>
                </td>
            </tr>
            }
        </tbody>
    </table>
</div>

