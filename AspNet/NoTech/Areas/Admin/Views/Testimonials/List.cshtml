@model IList<Testimonials>

@if (TempData["Success"] != null)
{
    <div class="alert alert-primary" role="alert">
        @TempData["Success"]
    </div>
}

<div class="table-responsive scrollbar">
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Name</th>
                <th scope="col">Text</th>
                <th scope="col">Company</th>
                <th scope="col">Image</th>

                <th class="text-end" scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.ID</td>
                    <td>@item.Name</td>
                    <td>@item.Text</td>
                    <td>@item.Company</td>
                    <td>
                        <img width="200" height="150" src="~/assets/images/testimonial/@item.Image">
                    </td>

                    <td class="text-end">
                        <div>
                            <a href="/Admin/Testimonials/Edit/@item.ID"><i class="fas fa-edit"></i></a>
                            <a href="/Admin/Testimonials/Delete/@item.ID" onclick="return confirm('Are you delete?')"><i class="fas fa-trash-alt"></i></a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

